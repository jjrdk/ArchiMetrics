<UserControl x:Class="ArchiMetrics.UI.View.AboutView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:support="clr-namespace:ArchiMetrics.UI.Support"
			 xmlns:localization="clr-namespace:ArchiMetrics.UI.Localization"
			 mc:Ignorable="d"
			 d:DesignHeight="300"
			 d:DesignWidth="300">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0"
				   Text="{x:Static localization:Strings.AppTitle}"
				   FontSize="40pt"
				   Margin="5" />
		<TextBlock Grid.Row="1"
				   Text="{x:Static localization:Strings.Copyright}"
				   FontSize="14pt"
				   Margin="5" />
		<TextBlock Grid.Row="2"
				   Text="{x:Static localization:Strings.InstalledPrograms}"
				   FontSize="12pt"
				   Margin="5" />
		<ListBox Grid.Row="3"
				 Margin="5"
				 ItemsSource="{Binding Source={x:Static support:InstalledPackages.Instance}}" />
		<TextBlock Grid.Row="4"
				   TextWrapping="Wrap"
				   Margin="5"
				   Text="{x:Static localization:Strings.LegalWarning}" />
	</Grid>
</UserControl>
