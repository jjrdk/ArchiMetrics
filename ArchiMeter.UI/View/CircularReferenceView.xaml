<UserControl x:Class="ArchiMeter.UI.View.CircularReferenceView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:ArchiMeter.UI.ViewModel"
             mc:Ignorable="d"
			 d:DesignHeight="300"
			 d:DesignWidth="300"
			 d:DataContext="{d:DesignInstance Type=viewModel:CircularReferenceViewModel}">
	<Grid>
		<DataGrid ItemsSource="{Binding Path=CircularReferences}"
				  AutoGenerateColumns="False"
				  VerticalAlignment="Stretch"
				  HorizontalAlignment="Stretch"
				  IsReadOnly="True">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Root Edge"
									Binding="{Binding Path=Root}" />
				<DataGridTextColumn Header="Last Edge"
									Binding="{Binding Path=LastEdge}" />
				<DataGridTextColumn Header="Length"
									Binding="{Binding Path=ChainLength}" />
				<DataGridTextColumn Header="Chain"
									Binding="{Binding}" />
			</DataGrid.Columns>
		</DataGrid>
		<ProgressBar Grid.ColumnSpan="2"
					 Margin="100,0,25,0"
					 Height="25"
					 MinWidth="200"
					 IsIndeterminate="True"
					 Visibility="{Binding Path=IsLoading, Converter={StaticResource TrueVisibility}}"
					 VerticalAlignment="Center"
					 HorizontalAlignment="Stretch" />
	</Grid>
</UserControl>
