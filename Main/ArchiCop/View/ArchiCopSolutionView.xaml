<UserControl x:Class="ArchiCop.View.ArchiCopSolutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:ArchiCop.ViewModel" xmlns:view="clr-namespace:ArchiCop.View"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Control.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="..\MainWindowResources.xaml" />
                <ResourceDictionary>
                    <DataTemplate DataType="{x:Type viewModel:GraphCommandViewModel}">
                        <view:GraphCommandView />
                    </DataTemplate>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Control.Resources>

    <Grid>
        <StackPanel>
            <HeaderedContentControl Header="{Binding DisplayName}" Style="{StaticResource MainHCCStyle}" />
            <ComboBox ItemsSource="{Binding Files}" IsSynchronizedWithCurrentItem="True" />
            <HeaderedContentControl Content="{Binding Commands}" ContentTemplate="{StaticResource CommandsTemplate}" />
        </StackPanel>
    </Grid>
</UserControl>