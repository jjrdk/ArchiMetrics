<UserControl x:Class="ArchiCop.View.GraphEdgesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    <ListView ItemsSource="{Binding EdgesToVisualize}">
        <ListView.View>
            <GridView>
                <GridViewColumn Width="200" Header="Source" DisplayMemberBinding="{Binding Source.Name}" />
                <GridViewColumn Width="200" Header="Target" DisplayMemberBinding="{Binding Target.Name}" />
            </GridView>
        </ListView.View>
    </ListView>

</UserControl>
