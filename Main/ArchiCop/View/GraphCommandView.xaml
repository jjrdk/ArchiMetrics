<UserControl x:Class="ArchiCop.View.GraphCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <HeaderedContentControl Header="{Binding}">
            <HeaderedContentControl.HeaderTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5,5,5,5">
                        <Image Source="{Binding ImageSource}" Height="32" Width="32" HorizontalAlignment="Left" />
                        <TextBlock Margin="2,6">
                            <Hyperlink Command="{Binding Path=ShowCommand}">
                                <TextBlock Text="{Binding DisplayName}" />
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </HeaderedContentControl.HeaderTemplate>

        </HeaderedContentControl>
    </Grid>
</UserControl>