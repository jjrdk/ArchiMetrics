<?xml version="1.0" encoding="utf-8"?>

<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>

    <MSBuildCommunityTasksPath>$(WorkingDirectory)\Build</MSBuildCommunityTasksPath>
		<ProjectName>ArchiCop</ProjectName>		
		<BuildNumber Condition="'$(BuildNumber)' == ''" />

		<ReleaseName>$(ProjectName) $(BuildNumber)</ReleaseName>

		<ReleaseZipFileName>$(WorkingDirectory)\$(ReleaseName).Zip</ReleaseZipFileName>
		<SourceZipFileName>$(WorkingDirectory)\Source $(ReleaseName).Zip</SourceZipFileName>
		
	</PropertyGroup>	

</Project>